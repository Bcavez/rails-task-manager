<h1>My Tasks</h1>
<% @tasks.each do |task| %>
  <div>
  <p><%= check_box_tag 'completed', 'completed', task[:completed], disabled: true %></p>
  <p><%= link_to task[:title], task_path(task) %></p>
  </div>
<% end %>
